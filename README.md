# 🏨 نظام إدارة الفندق المتكامل

نظام متكامل لإدارة الغرف الفندقية مع نظام QR Code للزوار وقاعدة بيانات NoSQL محلية.

## 📋 المميزات

### 1. صفحة عرض الغرف للزوار (`index.html`)
- ✅ عرض جميع الغرف مع التمييز اللوني:
  - **أخضر**: الغرف المتاحة للحجز
  - **أحمر**: الغرف المحجوزة
  - **برتقالي**: الغرف تحت الصيانة
- 📊 إحصائيات فورية للغرف
- 🔍 تصفية الغرف (الكل / المتاحة / المحجوزة)
- 📱 تصميم متجاوب يعمل على جميع الأجهزة
- 🔄 تحديث تلقائي كل 30 ثانية

### 2. صفحة QR Code (`qrcode.html`)
- 📱 توليد QR Code تلقائياً
- 🖨️ إمكانية طباعة الكود
- 📋 نسخ الرابط بضغطة واحدة
- 📝 تعليمات واضحة للاستخدام
- ✨ تصميم جذاب وسهل الاستخدام

### 3. لوحة التحكم (`admin.html`)
- ➕ إضافة غرف جديدة
- ✏️ تعديل بيانات الغرف
- 🗑️ حذف الغرف
- 📊 عرض جميع الغرف في جدول تفاعلي
- 💾 حفظ البيانات في قاعدة بيانات NoSQL محلية

### 4. قاعدة بيانات NoSQL محلية (`db.js`)
- 💾 **IndexedDB** - قاعدة بيانات NoSQL مدمجة في المتصفح
- 🔄 **CRUD كامل** - إنشاء، قراءة، تحديث، حذف
- 📝 **تسجيل العمليات** - سجل كامل لجميع التغييرات
- 🔍 **فهرسة متقدمة** - البحث حسب الحالة والنوع
- 📊 **إحصائيات** - معلومات شاملة عن قاعدة البيانات
- ⚡ **سرعة عالية** - أداء ممتاز بدون خادم
- 🔒 **آمنة** - البيانات محفوظة محلياً في المتصفح

## 🚀 كيفية الاستخدام

### 1. للزوار (استخدام QR Code):
1. افتح ملف `qrcode.html` في المتصفح
2. اطبع الصفحة أو اعرضها على شاشة
3. عند مسح QR Code بالهاتف، سيتم فتح صفحة الغرف المتاحة
4. الغرف المتاحة ستظهر باللون الأخضر

### 2. لإدارة الفندق:
1. افتح ملف `admin.html` في المتصفح
2. أضف أو عدل الغرف حسب الحاجة
3. البيانات تُحفظ تلقائياً في قاعدة بيانات NoSQL محلية

## 📁 هيكل الملفات

```
hotel-system/
├── index.html          # صفحة عرض الغرف للزوار
├── qrcode.html         # صفحة QR Code
├── admin.html          # لوحة التحكم
├── welcome.html        # صفحة الترحيب
├── app.js             # منطق عرض الغرف
├── admin.js           # منطق إدارة الغرف
├── db.js              # قاعدة البيانات NoSQL
├── styles.css         # التنسيقات
└── README.md          # هذا الملف
```

## 🗄️ قاعدة البيانات NoSQL

### البنية:
```javascript
// مخزن الغرف (Rooms Store)
{
  number: 101,           // رقم الغرفة (Primary Key)
  type: 'غرفة مفردة',    // نوع الغرفة
  status: 'available',   // الحالة (available/occupied/maintenance)
  price: 150            // السعر بالريال
}

// مخزن السجلات (Logs Store)
{
  id: 1,                        // معرف تلقائي
  action: 'add',                // نوع العملية
  room: 101,                    // رقم الغرفة
  timestamp: '2026-02-01...',   // وقت العملية
  details: 'تم إضافة الغرفة 101' // تفاصيل
}
```

### العمليات المتاحة:
- `addRoom(room)` - إضافة غرفة جديدة
- `updateRoom(room)` - تحديث غرفة موجودة
- `deleteRoom(roomNumber)` - حذف غرفة
- `getRoom(roomNumber)` - الحصول على غرفة محددة
- `getAllRooms()` - الحصول على جميع الغرف
- `getRoomsByStatus(status)` - الحصول على الغرف حسب الحالة
- `getRoomsByType(type)` - الحصول على الغرف حسب النوع
- `getLogs(limit)` - الحصول على سجل العمليات
- `getStats()` - الحصول على الإحصائيات
- `clearAll()` - مسح جميع البيانات
3. التغييرات ستنعكس مباشرة على صفحة الزوار

### 3. معاينة الغرف:
1. افتح ملف `index.html` في المتصفح
2. استعرض الغرف المتاحة والمحجوزة
3. استخدم الفلاتر للبحث

## 📁 هيكل الملفات

```
hotel-system/
├── index.html          # صفحة عرض الغرف للزوار
├── qrcode.html         # صفحة QR Code
├── admin.html          # لوحة التحكم
├── app.js             # منطق عرض الغرف
├── admin.js           # منطق إدارة الغرف
├── styles.css         # التنسيقات
└── README.md          # هذا الملف
```

## 🎨 الألوان المستخدمة

- **أخضر**: `#11998e` → `#38ef7d` (متاح)
- **أحمر**: `#ee0979` → `#ff6a00` (محجوز)
- **برتقالي**: `#ffa500` → `#ff8c00` (صيانة)
- **أزرق**: `#667eea` → `#764ba2` (الأزرار والعناوين)

## 💡 نصائح الاستخدام

1. **لطباعة QR Code بجودة عالية:**
   - افتح `qrcode.html`
   - اضغط على زر "طباعة الكود"
   - اختر "حفظ كـ PDF" للحصول على جودة أفضل

2. **لتخصيص الأسعار:**
   - افتح `admin.html`
   - عدل السعر لكل غرفة
   - احفظ التغييرات

3. **للاستخدام على شبكة محلية:**
   - ضع الملفات على خادم محلي
   - شارك الرابط مع الأجهزة المتصلة بنفس الشبكة

## 🔧 التطويرات المستقبلية

- [ ] ربط قاعدة بيانات حقيقية
- [ ] نظام حجز مباشر
- [ ] إشعارات فورية
- [ ] تقارير وإحصائيات متقدمة
- [ ] نظام دفع إلكتروني
- [ ] دعم لغات متعددة

## 📱 المتطلبات

- متصفح حديث يدعم HTML5 و JavaScript
- لا يحتاج إلى خادم (يعمل محلياً)
- اتصال بالإنترنت لتحميل مكتبة QR Code فقط

## 📞 الدعم

إذا واجهت أي مشكلة أو لديك اقتراحات، لا تتردد في التواصل.

---

**تم التطوير بـ ❤️ لتسهيل إدارة الفنادق**
