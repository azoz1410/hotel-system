# 🚀 دليل الإعداد السريع

## خطوات التشغيل السريع (5 دقائق)

### 1️⃣ تفعيل Firebase Authentication

**في Firebase Console:**
```
1. اذهب إلى: https://console.firebase.google.com
2. اختر مشروعك (hotel-system)
3. من القائمة الجانبية → Authentication
4. اضغط "Get started"
5. اختر "Email/Password"
6. فعّل "Email/Password" واضغط Save
```

### 2️⃣ إنشاء مستخدم إداري

**في نفس صفحة Authentication:**
```
1. اضغط تبويب "Users"
2. اضغط "Add user"
3. أدخل:
   - Email: admin@hotel.com
   - Password: admin123
4. اضغط "Add user"
```

### 3️⃣ تحديث قواعد الأمان

**في Firebase Console → Realtime Database → Rules:**
```json
{
  "rules": {
    "rooms": {
      ".read": true,
      ".write": "auth != null"
    },
    "bookings": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "customers": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "logs": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```
ثم اضغط **"Publish"**

### 4️⃣ تشغيل النظام محلياً

**في Terminal:**
```bash
cd /Users/aziz5g/Desktop/hotel-system
python3 -m http.server 8000
```

**افتح المتصفح:**
```
http://localhost:8000/welcome.html
```

---

## ✅ اختبار النظام

### 1. اختبر تسجيل الدخول:
```
1. افتح: http://localhost:8000/login.html
2. أدخل:
   - Email: admin@hotel.com
   - Password: admin123
3. اضغط تسجيل الدخول
4. يجب أن تُنقل للوحة التحكم
```

### 2. اختبر إضافة غرفة:
```
1. في لوحة التحكم
2. أدخل:
   - رقم الغرفة: 101
   - النوع: غرفة مفردة
   - الحالة: متاحة
   - السعر: 200
3. اضغط "إضافة غرفة"
4. يجب أن تظهر Toast خضراء
```

### 3. اختبر نظام الحجوزات:
```
1. اذهب إلى: الحجوزات (من لوحة التحكم)
2. املأ نموذج الحجز:
   - اختر الغرفة: 101
   - الاسم: محمد أحمد
   - الجوال: 0512345678
   - تاريخ الدخول: غداً
   - تاريخ الخروج: بعد 3 أيام
3. السعر يُحسب تلقائياً
4. اضغط "تأكيد الحجز"
```

### 4. اختبر لوحة الإحصائيات:
```
1. اذهب إلى: الإحصائيات
2. يجب أن تشاهد:
   - إحصائيات الغرف
   - رسوم بيانية ملونة
   - الإيرادات
3. اضغط "تصدير التقرير" لتنزيل JSON
```

---

## 🎯 الصفحات المتاحة

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الرئيسية | `/welcome.html` | صفحة البداية |
| تسجيل الدخول | `/login.html` | دخول الإدارة |
| عرض الغرف | `/index.html` | للزوار |
| لوحة التحكم | `/admin.html` | إدارة الغرف |
| الحجوزات | `/bookings.html` | نظام الحجوزات |
| الإحصائيات | `/dashboard.html` | التقارير |
| QR Code | `/qrcode.html` | للطباعة |

---

## 🔐 بيانات الدخول الافتراضية

```
البريد الإلكتروني: admin@hotel.com
كلمة المرور: admin123
```

⚠️ **تحذير:** غيّر هذه البيانات في الإنتاج!

---

## 🐛 حل المشاكل السريع

### المشكلة: "Firebase is not defined"
**الحل:** تأكد من الاتصال بالإنترنت (Firebase يُحمّل من CDN)

### المشكلة: "Auth error"
**الحل:** تأكد من تفعيل Email/Password في Firebase Console

### المشكلة: "Permission denied"
**الحل:** حدّث قواعد Firebase من الخطوة 3

### المشكلة: الصفحة لا تفتح
**الحل:** تأكد من تشغيل الخادم المحلي (python3 -m http.server)

---

## 📱 تثبيت كتطبيق على الهاتف

### Android (Chrome):
```
1. افتح الموقع
2. اضغط ⋮ (القائمة)
3. اختر "Add to Home screen"
4. اضغط "Add"
```

### iOS (Safari):
```
1. افتح الموقع
2. اضغط زر المشاركة
3. اختر "Add to Home Screen"
4. اضغط "Add"
```

---

## 🎨 تخصيص سريع

### تغيير اللون الرئيسي:
**في styles.css:**
```css
/* ابحث عن: */
#667eea

/* استبدل بـ: */
#YOUR_COLOR
```

### تغيير اسم الفندق:
**في جميع الملفات، ابحث عن:**
```
نظام إدارة الفندق
```
**واستبدله باسم فندقك**

---

## 📊 البيانات التجريبية

### إضافة غرف تجريبية:
```
غرفة 101 - غرفة مفردة - 200 ريال
غرفة 102 - غرفة مزدوجة - 300 ريال
غرفة 103 - جناح - 500 ريال
غرفة 201 - غرفة عائلية - 400 ريال
```

---

## 🚀 الخطوات التالية

بعد الإعداد الأساسي:

1. **إضافة غرفك** من لوحة التحكم
2. **اطبع QR Code** وضعه في الاستقبال
3. **اختبر الحجوزات** مع عميل تجريبي
4. **راجع الإحصائيات** يومياً
5. **صدّر التقارير** شهرياً

---

## 📞 الدعم

إذا واجهت مشكلة:
1. راجع [UPDATES.md](UPDATES.md)
2. راجع [README.md](README.md)
3. راجع [FIREBASE-SETUP.md](FIREBASE-SETUP.md)
4. افتح Console (F12) وابحث عن الأخطاء

---

## ✨ نصائح احترافية

1. **النسخ الاحتياطي:**
   ```
   صدّر التقرير من لوحة الإحصائيات يومياً
   ```

2. **الأمان:**
   ```
   غيّر كلمة المرور بانتظام
   لا تشارك بيانات الدخول
   ```

3. **الأداء:**
   ```
   احذف السجلات القديمة شهرياً
   راقب عدد الحجوزات المعلقة
   ```

---

**🎉 الآن نظامك جاهز للعمل!**

افتح [welcome.html](http://localhost:8000/welcome.html) وابدأ! 🚀
