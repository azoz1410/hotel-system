# 🚀 دليل البدء السريع

## كيفية تشغيل النظام

### الطريقة 1: فتح الملفات مباشرة
1. افتح ملف `welcome.html` في متصفحك
2. ستجد 3 أزرار:
   - **صفحة الباركود**: للطباعة وعرضها للزوار
   - **عرض الغرف**: لمعاينة الغرف المتاحة
   - **لوحة التحكم**: لإدارة الغرف

### الطريقة 2: استخدام خادم محلي بسيط

#### على macOS/Linux:
```bash
cd /Users/aziz5g/Desktop/hotel-system
python3 -m http.server 8000
```

ثم افتح المتصفح على: `http://localhost:8000/welcome.html`

#### على Windows:
```bash
cd C:\path\to\hotel-system
python -m http.server 8000
```

## 📝 خطوات الاستخدام

### للزوار:
1. امسح QR Code بكاميرا هاتفك
2. سيفتح صفحة الغرف تلقائياً
3. الغرف المتاحة ستظهر باللون الأخضر ✅

### للإدارة:
1. افتح `admin.html`
2. أضف أو عدل الغرف
3. التغييرات تحفظ تلقائياً

## 🎯 الملفات المهمة

- `welcome.html` - صفحة البداية ⭐
- `qrcode.html` - صفحة الباركود 📱
- `index.html` - عرض الغرف 🏠
- `admin.html` - لوحة التحكم ⚙️

## ⚡ نصائح سريعة

1. **لطباعة QR Code:**
   - افتح `qrcode.html`
   - اضغط Ctrl+P (Cmd+P على Mac)
   - اطبع أو احفظ كـ PDF

2. **لتغيير بيانات الغرف:**
   - افتح `admin.html`
   - عدل كما تريد
   - البيانات تحفظ تلقائياً في المتصفح

3. **لإعادة تعيين البيانات:**
   - افتح Console في المتصفح (F12)
   - اكتب: `localStorage.clear()`
   - أعد تحميل الصفحة

## 🌐 للاستخدام على شبكة WiFi

إذا أردت أن يصل الزوار للنظام عبر WiFi:

1. شغل خادم محلي كما في الأعلى
2. اعرف عنوان IP الخاص بجهازك:
   - Mac: `ifconfig | grep "inet "`
   - Windows: `ipconfig`
3. استخدم العنوان: `http://[your-ip]:8000`
4. أنشئ QR Code جديد بهذا العنوان

## ❓ المشاكل الشائعة

**المشكلة:** QR Code لا يعمل
**الحل:** تأكد من استخدام خادم HTTP (وليس file://)

**المشكلة:** التغييرات لا تحفظ
**الحل:** تأكد من تفعيل localStorage في المتصفح

**المشكلة:** الألوان لا تظهر
**الحل:** تأكد من تحميل ملف `styles.css` بشكل صحيح

## 📞 الدعم

إذا واجهت أي مشكلة، راجع ملف `README.md` للمزيد من التفاصيل.

---
**✨ استمتع باستخدام النظام!**
